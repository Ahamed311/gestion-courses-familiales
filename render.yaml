services:
  - type: web
    name: gestion-courses
    env: node
    plan: free
    branch: main
    buildCommand: npm install
    startCommand: node backend/index.js
    envVars:
      - key: NODE_ENV
        value: production